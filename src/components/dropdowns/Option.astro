---
interface Props {
  value: string
  text: string
  selected?: boolean
  isPlaceholder?: boolean
}

const { value, text, selected, isPlaceholder } = Astro.props
---

<option
  class={`bg-white text-sm ${isPlaceholder ? 'is-placeholder' : ''} text-black`}
  {value}
  {selected}
  disabled={isPlaceholder === true}
  hidden={isPlaceholder === true}
>
  {text}
</option>
